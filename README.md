# DBClass_RelationalAlgebra

```python
$ rep \leftarrow \Pi{(customerName, city, salesRepEmployeeNumber)}(\sigma_{classicmodels.customers}(creditLimit > 50000 \wedge country = France) (classicmodels \ costumers) $
```
